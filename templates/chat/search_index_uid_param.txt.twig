{% if allIndexUids|length == 1 %}Always use the index "{{ allIndexUids[0] }}". Do not search any other index.
{% else %}The index to search. Must be one of: {{ allIndexUids|join(', ') }}. Do not use any other index.
{% endif %}